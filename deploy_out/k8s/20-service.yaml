apiVersion: v1
kind: Service
metadata:
  name: llmserve
  namespace: llmserve
  labels:
    app.kubernetes.io/name: llmserve
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: llmserve
    app.kubernetes.io/component: api
  ports:
    - name: http
      port: 8000
      targetPort: http
    - name: metrics
      port: 9400
      targetPort: metrics
